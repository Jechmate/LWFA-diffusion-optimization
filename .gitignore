# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# ---------------------------------------------------------------------------
# Run outputs and analysis artifacts.
# These are reproducible from the scripts in this repo and are kept locally
# rather than published.
# ---------------------------------------------------------------------------
comparison_*/
match_spectrum_*/
multi_*/
cosine_annealing_*/
hybrid_optimization_*/
lbfgs/
exclusion_evaluation_results/
results/
runs/
plots/

# Analysis tables and scan caches (root level only, so imgs/ is unaffected)
approach_comparison.csv
distribution_eval.csv
wasserstein_loo.csv
results.txt
spectrum_target_*.csv
/*.npz
/*.png

# ---------------------------------------------------------------------------
# Model checkpoints. Only the final EMA checkpoint is published; the optimiser
# states and intermediate epochs are not needed to reproduce any result.
# ---------------------------------------------------------------------------
models/*
!models/edm_4kepochs
models/edm_4kepochs/*
!models/edm_4kepochs/ema_ckpt_final.pt

# ---------------------------------------------------------------------------
# OS
# ---------------------------------------------------------------------------
.DS_Store
